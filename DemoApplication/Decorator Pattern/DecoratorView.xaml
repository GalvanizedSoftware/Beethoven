<UserControl x:Class="GalvanizedSoftware.Beethoven.DemoApp.Decorator_Pattern.DecoratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:decoratorPattern="clr-namespace:GalvanizedSoftware.Beethoven.DemoApp.Decorator_Pattern"
             Height="350" Width="525" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=decoratorPattern:DecoratorViewModel, IsDesignTimeCreatable=False }">
  <StackPanel>
    <Button Command="{Binding AddLaptopCommand}">Add Laptop</Button>
    <Button Command="{Binding AddMouseCommand}">Add Mouse</Button>
    <Button Command="{Binding AddGiftWrapping}">Add Gift Wrapping</Button>
    <ItemsControl ItemsSource="{Binding OrderItems}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal">
            <TextBlock Margin="5,0" Text="{Binding Name}"/>
            <TextBlock Margin="5,0" Text="{Binding Price}"/>
            <TextBlock Margin="5,0" Text="{Binding Weight}"/>
          </StackPanel>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
</UserControl>